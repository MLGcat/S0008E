file(GLOB TEXTURES "*.bmp" "*.png")
FILE(COPY  DESTINATION "${CMAKE_BINARY_DIR}/Textures")
FILE(COPY *.png DESTINATION "${CMAKE_BINARY_DIR}/Textures")

FILE(COPY *.obj DESTINATION "${CMAKE_BINARY_DIR}/Models")

FILE(COPY *.isf DESTINATION "${CMAKE_BINARY_DIR}/Shaders")
